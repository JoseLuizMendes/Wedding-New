# API Configuration
# Base URL for the Java/Spring Boot backend API
# Leave empty for same-origin requests (default)
# For external API, set the full URL e.g. http://localhost:8080
NEXT_PUBLIC_API_URL=

# Revalidation Secret
# Used for on-demand revalidation of cached pages
REVALIDATION_SECRET=your-secret-key-here
