# API Configuration
# Base URL for the backend API. Leave empty for same-origin requests (default).
# For external API, set the full URL e.g. http://localhost:8080
NEXT_PUBLIC_API_URL=

# Revalidation
REVALIDATION_SECRET=your-secret-key
